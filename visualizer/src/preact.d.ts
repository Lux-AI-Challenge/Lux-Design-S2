// preact doesn't have all the types that `@types/react` provides
// so we'll just use React types for Typescript even though we're actually using `preact` + `preact/compat` in runtime
// import JSX = preact.JSX
